/**
 * 1. Create a context provider file
 * 2. Create a context and set provider
 * 3. set the children props
 * 4. set the context value
 * 5. set the provider
 * so basically Im setting the whole app as a child and sending it from main.jsx to the auth provider then reciving it as a children, then I can sent the context inside the whole app
*/